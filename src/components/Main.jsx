import { motion } from 'framer-motion';
import { styles } from '../styles.js';
import { ComputersCanvas } from './canvas';

const Main = () => {
  return (
    <section className='relative w-full h-screen mx-auto'></section>
  )
}

export default Main;